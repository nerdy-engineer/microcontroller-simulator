# microcontroller-simulator
This is NOT an emulator for any particular microcontroller. It IS a generic simulator for microcontroller outputs. This is for embedded code prototyping, not for electrical simulation (yet! Maybe eventually).

The goal is to have a library built such that you can create a microcontroller object configured from a (xml? json?) file.


